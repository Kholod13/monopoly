/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        allowedDevOrigins: ['26.228.5.142'], // Разрешаем твой IP для Hot Reload
    },
};

export default nextConfig;